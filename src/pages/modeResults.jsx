import React from "react";
// import { useParams } from 'react-router-dom';

const ModeResults = () => {
    // const { img_id, model_results } = useParams();
    // const [results, setResults] = useState(model_results);
    // const [img, setImg] = useState(img_id);
    
    return <div>ModeResults</div>;
};  

export default ModeResults;